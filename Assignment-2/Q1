/*
Ans-1
     In CSS, the term "box model" is used when talking about design and layout.
     The CSS box model is a container that contains multiple properties including borders, margin, padding, and the content itself. According to the CSS box model, the web browser supplies each element as a square prism.
     
     */
     